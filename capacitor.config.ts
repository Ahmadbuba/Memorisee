import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ahmad.memorisee',
  appName: 'memorisee',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
